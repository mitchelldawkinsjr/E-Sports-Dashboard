# Esports Dashboard - Production Environment Variables
# Fill in your actual values below

# Database Configuration
DB_DATABASE=esports
DB_USERNAME=esports
DB_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD

# Application URLs (replace yourdomain.com with your actual domain)
APP_URL=https://yourdomain.com
FRONTEND_URL=https://yourdomain.com
NEXT_PUBLIC_API_URL=https://yourdomain.com/api

# Sanctum Configuration (replace with your domain, no https://)
SANCTUM_STATEFUL_DOMAINS=yourdomain.com,www.yourdomain.com

# CORS Configuration (replace with your domain, include https://)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Application Settings
APP_ENV=production
APP_DEBUG=false

# Redis Configuration (Internal - Don't Change)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_CLIENT=predis

# Session & Cache
SESSION_DRIVER=redis
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis
